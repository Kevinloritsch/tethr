import { View, Text } from 'react-native';

// this can be the standard file for loading?
const Loading = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Loading;
