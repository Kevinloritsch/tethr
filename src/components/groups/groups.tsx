import { View, Text } from 'react-native';
import Group from '@/components/groups/group';

const Groups = () => {
  return (
    <View className="m-6">
      <Group/>
    </View>
  );
};

export default Groups;
